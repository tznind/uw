/**
 * Common Equipment Card - Available to all characters via Everyone role
 */

// Ensure CardInitializers namespace exists
window.CardInitializers = window.CardInitializers || {};

// Export initialization function for the card system to call
window.CardInitializers.common = function() {
    // This card provides basic equipment tracking for all characters
    // All inputs are automatically persisted by the main system
    
    console.log('Common Equipment card initialized - providing universal gear access');
    
    // Any custom initialization logic would go here
    // For now, this card just provides basic form inputs that are auto-persisted
};
